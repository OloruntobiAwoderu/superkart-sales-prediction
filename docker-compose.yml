services:
  backend:
    build: ./backend
    container_name: backend
    ports:
      - "7860:7860"
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: frontend
    depends_on:
      - backend
    ports:
      - "8501:8501"
    restart: unless-stopped
